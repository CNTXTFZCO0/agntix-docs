{
  "name": "agntix-docs-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Public Agentix developer portal (Mintlify). Hand-written guides + an OpenAPI spec synced from agntix-gateway with internal endpoints stripped.",
  "scripts": {
    "dev": "mint dev",
    "broken-links": "mint broken-links",
    "sync:openapi": "tsx scripts/sync-openapi.ts",
    "lint:internal": "tsx scripts/lint-internal-paths.ts",
    "check": "pnpm run broken-links && pnpm run lint:internal"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "axios": "^1.7.7",
    "mint": "^4.0.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
